import { StyleSheet } from "react-native";

export default StyleSheet.create({
      infoRow: {
        flexDirection: "row",
        justifyContent: "space-between",
        marginBottom: 4,
      },
      keyText: {
        fontWeight: "bold",
      },
      valueText: {
        color: "#555",
      },

    
})